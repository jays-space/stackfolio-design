# screens/

Every StackFolio product screen, plus the canonical `Header.dc.html` and `Footer.dc.html`.

The chrome lives here rather than at the folder root because imports resolve to same-folder
siblings, and these screens are its only consumers. One copy, no resync.
